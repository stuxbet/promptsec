## Summary

What this PR changes, in 1-3 bullets.

## Why

The motivation. Link the issue if there is one (e.g. `Closes #12`).

## Test plan

- [ ] `pytest` passes against my local LLM endpoint
- [ ] `python -m src.cli.main evaluate --scenario-set default` runs to completion
- [ ] Manual verification: ...

## Screenshots / output

If this changes an evaluation metric or visible CLI output, paste the before/after.

## Risk

- [ ] Touches the defended pipeline
- [ ] Changes the scoring rubric
- [ ] Changes the synthetic dataset
- [ ] Adds a new external dependency
