[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "promptsec"
version = "1.0.0"
description = "Lab benchmark of indirect prompt-injection defenses for an internal AI assistant."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
  { name = "Luke Malcom" }
]
dependencies = [
  "httpx>=0.27,<1",
  "matplotlib>=3.8,<4",
  "pydantic>=2.7,<3",
  "pytest>=8.0,<9",
  "typer>=0.12,<1"
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0,<9"
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
